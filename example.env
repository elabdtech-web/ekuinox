# Example environment variables for ekuinox
# Copy this file to `.env` (backend) or to the appropriate env file used by your deployment
# Do NOT commit real secrets to version control. Replace placeholder values with your real ones.

# ---------------------------
# Server / Backend
# ---------------------------
# Port for backend server
PORT=5001
# Node environment: development | production
NODE_ENV=development
# MongoDB connection string
MONGO_URI=mongodb://localhost:27017/ekuinox
# JWT secret used for signing auth tokens
JWT_SECRET=your_jwt_secret_here
# Token expiration (example: 7d, 24h)
JWT_EXPIRES_IN=7d

# ---------------------------
# API / Frontend
# ---------------------------
# Frontend (Vite) API base URL used in client (example: http://localhost:5001/api)
VITE_API_URL=http://localhost:5001/api

# Note: API URL is configured in src/config/api.js
# This avoids process.env issues in the browser

# Optional: Enable/disable features
REACT_APP_ENABLE_REGISTRATION=true
REACT_APP_ENABLE_PASSWORD_RESET=true

# Development settings
REACT_APP_DEBUG=true

# ---------------------------
# Email / SMTP (for OTP, password reset, transactional emails)
# ---------------------------
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false # true for 465, false for 587
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_password
EMAIL_FROM="Ekuinox <no-reply@example.com>"

# ---------------------------
# Stripe (payments)
# ---------------------------
STRIPE_SECRET_KEY=sk_test_your_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key

# ---------------------------
# Optional 3rd-party / OAuth
# ---------------------------
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ---------------------------
# Example feature flags / misc
# ---------------------------
# Turn on verbose logging in backend
LOG_LEVEL=info
# Any other custom keys consumed by the app
# EXAMPLE_FEATURE_FLAG=false

# ---------------------------
# Usage notes
# ---------------------------
# 1. Copy this file to a .env in the backend root (or to whichever env file your deployment reads).
#    Example (PowerShell):
#      cp example.env .env
# 2. Replace the placeholder values with real credentials.
# 3. For frontend Vite-based projects you may want to set VITE_API_URL in the frontend environment
#    or in the hosting provider's environment variables.
# 4. Never commit your real .env to git. Keep example.env generic and safe to check-in.
